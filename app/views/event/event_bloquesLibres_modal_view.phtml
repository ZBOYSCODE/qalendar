<div class="modal-dialog" style="width: 35%;">
    <div class="modal-content">
        <div class="header-item">
            <strong><h4>Fecha Seleccionada: <?php print(date('d-m-Y',strtotime($pcData['fecha']))) ?></h4></strong>
        </div>
        <form id="bloqueForm" action="<?php print($this->url->get("gestionQa/seleccionarBloque")) ?>" method="POST" data-type="ajax">
          <div class="modal-body item" style="min-height:50vh;">

              <table class="table table-striped table-hover">
                <thead>
                  <th>Hora</th>
                  <th></th>
                </thead>
                <?php foreach ($pcData['disponibles'] as $val) {?>
                  <tr>
                    <td><?php print(date('H:i',strtotime($val))) ?></td>
                    <td><input type="radio" class="radio" name="horaSeleccionada" value="<?php print(date('H:i',strtotime($val))) ?>" </td>
                  </tr>
                <?php } ?>
              </table>
          </div>
        </form>

        <div class="modal-footer" style="font-size: 15px; font-family: Sans-Serif;">
            <div class="links-box">
                <a onclick="$('#bloqueForm').submit()" class="btn btn-success" >Guardar</a>
                <a class=" btn btn-default dismiss-item " href="javascript:void(0)" data-dismiss="modal">Cerrar</a>
            </div>
        </div>
    </div>
</div>